const many = require('./many');
const manySync = require('./many-sync');
const one = require('./one');
const oneSync = require('./one-sync');

module.exports = {
  one,
  oneSync,
  many,
  manySync,
};
